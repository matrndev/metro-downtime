on /, show all lines with active alerts and **past alerts on that day**
show (redirect to page?) with event details when clicked on a segment
make tooltips work on mobile
filter by event severity
ui for filtering by line/event and timeframe
dashboard-style stats on landing page (global percentage of downtime etc)


track each route delays (trip_updates gtfs)
- show vehicle headcode

DONE: don't send an alert in each chunk, only send whole alert once and send alert ids in chunks
DONE: show downtime segment colours based on event severity
DONE: remove active=true entirely from db (no reason to have it, extra liability)
DONE: filter by specific effects (not just 'important only')

---
save downtime to db
treat orphaned alerts (no longer on website but still show up as active in db)

use sql instead lol